This script can be used to run various tests with the
[iozone](https://www.iozone.org/) tool. In order to run the script, you can
mount a fuse directory using the fuse demo binary, copy this script to the
mounted directory, and then run the script within the directory. Command line
flags used for iozone can be found as a comment in the script file.
